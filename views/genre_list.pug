extends layout


block content
  h1(style="text-align: center ; background-color : gray ; color: white")= title
  .container   
    include aside.pug
       
    .section
        ul
            each genre in genre_list
                li
                    a(href=genre.url) #{genre.name}
            else
                li No genre in the list.        